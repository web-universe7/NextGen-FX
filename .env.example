# Copy this file to .env for local testing.
# On Render, DO NOT upload a .env file — add these as Environment Variables
# in the Render dashboard instead (Settings -> Environment).

# Any long random string. Used to sign login sessions (JWT).
JWT_SECRET=change_this_to_a_long_random_string

# Get these from https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=rzp_test_TTszaxFYWMWOf2
RAZORPAY_KEY_SECRET=tBtkUhYYVUlRyJ5zLnwLeH2a

# Optional. Render sets PORT automatically, this is only used locally.
PORT=3000

NODE_ENV=production
